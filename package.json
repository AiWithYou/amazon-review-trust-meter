{
  "name": "amazon-review-trust-meter",
  "version": "2.2.0",
  "private": true,
  "description": "Local multi-signal Amazon.co.jp review risk analysis Chrome extension",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node --check scoring-base.js && node --check scoring-features.js && node --check scoring.js && node --check content.js && npm test",
    "package": "pwsh -NoProfile -File scripts/package-extension.ps1 && pwsh -NoProfile -File scripts/package-extension.ps1 -Store",
    "package:verify": "pwsh -NoProfile -File scripts/verify-package.ps1 && pwsh -NoProfile -File scripts/verify-package.ps1 -Store",
    "benchmark": "node scripts/benchmark.cjs",
    "preview": "node scripts/preview.cjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
